原始未聚合查询耗时：
``` 
{
  "queryType": "groupBy",
  "dataSource": "dwm_request_src",
  "granularity": "hour",
  "dimensions": [
    "pappid",
    "appid",
    { "type": "default", "dimension": "path", "outputName": "request_path" },
    "method",
    "status_code"
  ],
  "aggregations": [
    {
      "type": "doubleSum",
      "name": "dur_max",
      "fieldName": "dur_max"
    },{
      "type": "doubleSum",
      "name": "dur_sum",
      "fieldName": "dur_sum"
    }
  ],
  "intervals": [
    "2019-01-11T09/2019-02-16T10"
  ],
  "context": {
    "groupByStrategy": "v2",  // 强制使用较慢的 V2 引擎
    "maxScatterGatherBytes": 1073741824,  // 允许更大数据传输
    "timeout": 60000  // 避免超时
  }
}
```
或者等价SQL:
```sql
SELECT
  TIME_FLOOR(__time, 'PT1H') AS "hour",  
  "pappid",
  "appid",
  "path" AS "request_path",                 
  "method",
  "status_code",
  SUM("dur_max") AS "dur_max",               
  SUM("dur_sum") AS "dur_sum"                
FROM dwm_request
WHERE
  __time BETWEEN TIMESTAMP '2019-01-11 09:00:00' 
    AND TIMESTAMP '2019-02-16 10:00:00'
GROUP BY 1, 2, 3, 4, 5, 6                
ORDER BY "hour" ASC   
limit 10
```
聚合查询耗时：
```
{
  "queryType": "groupBy",
  "dataSource": "dwm_request",
  "granularity": "hour",
  "dimensions": [
    "pappid",
    "appid",
    { "type": "default", "dimension": "path", "outputName": "request_path" },
    "method",
    "status_code"
  ],
  "aggregations": [
    {
      "type": "doubleSum",
      "name": "dur_max",
      "fieldName": "dur_max"
    },{
      "type": "doubleSum",
      "name": "dur_sum",
      "fieldName": "dur_sum"
    }
  ],
  "intervals": [
    "2019-01-11T09/2019-02-16T10"
  ],
  "limitSpec": {
    "type": "default",
    "limit": 1000,  // 返回所有结果时可设置为极大值
    "columns": [
      { "dimension": "__time", "direction": "descending" }  // 按时间倒序
    ]
  },
  "context": {
    "groupByStrategy": "v2",  // 强制使用较慢的 V2 引擎
    "maxScatterGatherBytes": 1073741824,  // 允许更大数据传输
    "timeout": 60000  // 避免超时
  }
}

```


## 评测2：只按时间维度分组
原始未聚合查询耗时：
```
SELECT
TIME_FLOOR(__time, 'PT1H') AS "hour",  -- 按小时分组
SUM(dur_sum) AS total_dur              -- 聚合计算
FROM dwm_request
WHERE
__time BETWEEN TIMESTAMP '2023-01-01 00:00:00'
AND TIMESTAMP '2023-01-02 00:00:00'  -- 时间范围
GROUP BY 1
ORDER BY "hour" ASC


SELECT
TIME_FLOOR(__time, 'PT1H') AS "hour",  -- 按小时分组
SUM(dur_sum) AS total_dur              -- 聚合计算
FROM dwm_request_src
WHERE
__time BETWEEN TIMESTAMP '2019-01-11 00:00:00'
AND TIMESTAMP '2023-01-02 00:00:00'  -- 时间范围
GROUP BY 1
ORDER BY "hour" ASC
```
聚合查询耗时：
```
{
  "queryType": "groupBy",
  "dataSource": "dwm_request",
  "granularity": "hour",

  "aggregations": [
    {
      "type": "doubleSum",
      "name": "dur_max",
      "fieldName": "dur_max"
    },{
      "type": "doubleSum",
      "name": "dur_sum",
      "fieldName": "dur_sum"
    }
  ],
  "intervals": [
    "2019-01-11T/2023-01-02T"
  ]
}
```



